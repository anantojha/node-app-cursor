{
  "name": "node-app",
  "version": "1.0.0",
  "private": true,
  "description": "Login frontend for GCP-FastAPI auth API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
